#DBConfig
db.address=jdbc:mysql://localhost:3306/progettofinale?useLegacyDatetimeCode=false&serverTimezone=UTC
db.user=root
db.psw=root

#SecurityDBConfig
# Questa configurazione serve soprattutto quando state sviluppando
# serve ad aggiornare la struttura del DB quando il programma si riavvia
spring.jpa.hibernate.ddl-auto=update
# Configurazioni base per specificare il tipo del DB, di conseguenza indirizzo, username e password
spring.datasource.url=jdbc:mysql://localhost:3306/demosecurity?useLegacyDatetimeCode=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.password=root

